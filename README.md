Markdown Notes Manager (Python CLI Project)

A command-line application to create, search, view, and delete markdown notes.
Each note is saved as a `.md` file with timestamp.

Features
- Create notes
- View all notes
- Search a note by title
- Delete notes
- Uses file handling (no database needed)
- Saves notes in Markdown format

Run the program
```bash
python main.py
